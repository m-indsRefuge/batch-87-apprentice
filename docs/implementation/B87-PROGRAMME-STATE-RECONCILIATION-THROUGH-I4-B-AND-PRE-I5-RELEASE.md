# B87 Programme-State Reconciliation Through I4-B and PRE-I5 Release

## 1. Record status

**Record type:** current operational programme-state reconciliation

**Record date:** 2026-08-02

**Authoring authority:** `AUTHORIZE B87-PRE-I5`

**Repository baseline:**

```text
repository: C:\Users\nolan\AIProjects\batch-87-apprentice
branch at release: main
commit: d8258520a53955e23834e362837088bd1acb12b1
upstream: origin/main
tree: ef5abadce3c7b5e1c9f9558f4a80877166536efa
tracked and untracked state: clean
```

**Current implementation release:**

```text
B87-PRE-I5
```

This record reconciles the repository's operational summary with Nolan's
explicit post-merge release instruction. It preserves earlier accepted records
as historical decision-time evidence and does not rewrite them.

This record does not accept the PRE-I5 implementation candidate that will be
produced under the release. Acceptance remains a later Nolan-Byte decision.

## 2. Governing continuity

This current-state overlay remains subordinate to and preserves:

1. the accepted B87-D0 architecture corpus;
2. the ratified pre-LLM implementation programme and ratification decision;
3. the accepted I1 through I4-A implementation and acceptance history;
4. the accepted I4-B contract and scoped V0 regression decision version 1.0;
5. the accepted B87-V0 closure and its reopening rules; and
6. all constitutional authority, permission, privacy, evidence, memory, and
   production-versus-experimental boundaries.

Older records that state `ACTIVE_OPERATOR_RELEASE: NONE` or describe I4-B as
unimplemented remain accurate evidence of their own decision-time state. They
must not be read as the live state after this reconciliation.

## 3. B87-I4-B closure identity

B87-I4-B - Provider-Neutral Model and Invocation Bridge is accepted,
externally validated, merged, and closed.

Its immutable accepted identity is:

```text
accepted implementation commit: fa1a1e3effd9e30a070486cf8b40930ae1c2c10e
merge commit:                   d8258520a53955e23834e362837088bd1acb12b1
accepted tree:                  ef5abadce3c7b5e1c9f9558f4a80877166536efa
merge pull request:             PR #15
```

The merge commit contains the exact accepted implementation tree.

The scoped external validation result is:

```text
V0-T01 through V0-T13: PASS
V0-T14: reconsidered, not triggered, and unauthorized
```

V0-T14 has no execution result under this transition. Its non-trigger does not
constitute a pass, failure, waiver, or authorization.

I4-B closure proves only the accepted provider-neutral bridge and deterministic
inactive/mock-provider boundary within the recorded validation scope. It does
not establish real-provider operation, candidate-model behaviour, model
suitability, memory efficacy, developmental compounding, or general production
readiness.

## 4. Active PRE-I5 release

Nolan issued exactly:

```text
AUTHORIZE B87-PRE-I5
```

The release activates only B87-PRE-I5 - Deterministic Evaluation
Infrastructure under the conditionally accepted pre-LLM contract.

The authorized implementation boundary is deterministic, non-model support
for:

- immutable candidate metadata registration without acceptance or activation;
- immutable evaluation configuration registration and hashing;
- deterministic fixture discovery, versioning, ordering, and fail-closed
  conflict detection;
- blinded repeated-run and condition planning, including ablation metadata;
- explicit enabled, withheld, and over-transfer conditions;
- score, critical-failure, incomplete, invalid, interrupted, and withheld
  result contracts;
- append-only persistence, exact reconstruction, and contradiction detection;
- deterministic reporting that preserves blinding and negative evidence; and
- a synthetic deterministic mock campaign with restart replay.

The release does not authorize model invocation or provider execution.

## 5. Explicitly inactive boundary

The following remain unimplemented or unauthorized by this release:

- B87-I5 and the final candidate suite;
- a real provider or candidate-model execution;
- model selection, admission, acceptance, or activation;
- model weights, model downloading, model loading, or model serving;
- host, port, endpoint, URL, credential, token, secret, executable path, or
  model path in PRE-I5 production contracts;
- Ollama, llama.cpp, LM Studio, vLLM, Transformers, remote APIs, or equivalent
  live-provider paths;
- learned evaluators, reward models, learned ranking, search-based promotion,
  training, fine-tuning, reinforcement learning, or adapters;
- hidden-reasoning, sentience, loyalty, or anthropomorphic scoring;
- Validation V1 execution;
- B87-E0, B87-E1, or B87-E2 implementation or execution;
- tools, autonomous action, or Apprentice Execute permission; and
- `SOUL.md` activation or authorship.

Model output remains proposal-only and non-authoritative. The Apprentice
permissions remain Observe and Analyse only. A candidate, score, evaluator,
benchmark, report, test, or Codex statement cannot accept or promote itself.

## 6. PRE-I5 completion boundary

PRE-I5 may be returned for Nolan-Byte review only after the authorized branch
contains an internally passing, reviewable implementation with:

- an ordered immutable migration after `0012` without changing migrations
  `0001` through `0012`;
- focused and complete regression validation;
- fresh and populated-`0012` upgrade evidence;
- rollback, migration-tamper, idempotency, foreign-key, SQLite-integrity,
  commit-visibility, and separate-process reconstruction evidence;
- deterministic mock-campaign replay and identical report regeneration;
- invalid and contradictory evidence rejection;
- production-versus-experimental dependency proof;
- static proof that no live provider or model execution path exists;
- a machine-readable evidence packet; and
- a clean committed working tree.

Completion of those checks does not constitute acceptance and does not release
B87-I5, external validation, a provider, a model, or an experimental phase.

## 7. Authority statement

Nolan retains final human and phase-release authority. Byte retains
architecture, synthesis, governance, and semantic-review authority. Codex is
authorized only to implement and internally validate the bounded PRE-I5 slice,
create its review candidate and evidence packet, and then stop.
